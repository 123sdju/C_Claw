# POSIX CLI docs

This directory owns the POSIX desktop CLI documentation: runtime config,
plugins, sandbox approval behavior, and interactive commands.

- [config.md](config.md): POSIX CLI runtime configuration.
