# 用户偏好

## 关于我

<!--
  编辑此部分，告诉 Agent 关于你的信息。
  例如："我是一名从事嵌入式系统开发的后端工程师。"
-->

我是一名 C 语言和系统编程的开发者，正在构建 C-Claw 项目。

## 编码风格

<!--
  编辑此部分，描述你偏好的编码风格。
  例如："我偏好 Tab 缩进，变量命名使用匈牙利表示法。"
-->

- 纯 C（C99 或更高），避免 C++ 特性
- 不需要冗余注释——代码应自文档化
- 函数保持专注且小巧
- 遵循本代码库已有的模式和约定

## 项目规则

<!--
  编辑此部分，添加项目专属规则。
  例如："绝不直接提交到 main 分支。始终使用特性分支。"
-->

- CMake 是构建系统——使用 `cmake -B build -S . && cmake --build build`
- 测试运行命令：`ctest --test-dir build/windows-cli --output-on-failure`
- 项目遵循 Ports & Adapters 架构——遵循 vtable 模式
- 所有公共头文件放在 `include/cc/` 下对应的子目录
- 源文件放在 `src/` 下匹配的目录结构中
